local states = {}

function states:init(player)
    --[[player:getExtraData().aimEntity = Entity.spawn(
        'player_aim',
        player:getPosition(),
        0,
        0,
        player
    )]]
end

return states